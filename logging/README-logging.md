
To direct Python program output to both the standard output (console) and a file, you can utilize the logging module or manually redirect sys.stdout.

1. Using the logging module (Recommended for complex logging):

The logging module provides a robust and flexible way to handle logging, allowing you to easily direct messages to multiple destinations.

